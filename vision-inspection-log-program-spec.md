# 비전 검사 결과 Log Program

> 제품 요구사항 문서(PRD) 및 소프트웨어 요구사항 명세서(SRS)  
> 버전: 0.1 MVP 초안 · 기준 시간대: Asia/Seoul · 상태: 개발 전 검토용

## 1. 프로젝트 개요

### 1.1 목적

비전 시스템의 검사 결과를 TCP/IP 소켓으로 송수신하고, 웹 화면에서 실시간 모니터링하며, 결과를 데이터베이스와 날짜별 TXT/XLSX 파일로 안전하게 기록하는 프로그램을 구축한다. J1C 계열 터미널 프로그램의 익숙한 사용 흐름을 참고하되, 산업 현장에서 상태와 장애를 빠르게 파악할 수 있는 더 단순하고 정돈된 화면을 제공한다.

### 1.2 핵심 목표

- TCP Server 또는 TCP Client 모드로 비전 시스템과 통신한다.
- 송신·수신 데이터를 터미널 형태로 실시간 표시한다.
- TXT 저장과 XLSX 저장을 각각 독립적으로 켜고 끈다.
- 날짜별 로그 파일을 자동 생성하고 자정에 새 파일로 전환한다.
- 웹 화면에서 통신, 저장, 로그, 일정, 사용자를 관리한다.
- ID/PASSWORD 인증과 Admin/Operator/Viewer 역할 권한을 적용한다.
- 통신 성공 여부와 파일 저장 성공 여부를 별도 상태로 기록한다.
- 저장 장애가 TCP 수신을 중단시키지 않도록 DB와 재시도 큐를 사용한다.

### 1.3 배포 가정

- MVP는 현장 PC 또는 산업용 PC 한 대에서 백엔드 서비스와 DB를 실행하고, 같은 PC 또는 내부망의 브라우저에서 접속하는 구성을 기본으로 한다.
- 백엔드는 웹 화면이 닫혀 있어도 TCP 통신과 로그 저장을 계속 수행한다.
- 단일 장비 MVP는 내장형 DB를 사용할 수 있으며, 다중 서버나 높은 동시성이 필요하면 서버형 DB로 전환한다.
- 인터넷 직접 공개보다 현장망 또는 VPN 내부 운영을 권장한다.

### 1.4 제품 트랙 (Web / Native PC)

이 문서(4~15장)는 **Web 트랙**(백엔드 서비스 + 브라우저 UI, 4.1 구조도 기준)을 정의한다. 이와 별도로 J1C 계열 사용자를 위한 **독립 실행형 네이티브 PC 앱** 트랙을 병행 제공한다.

- 두 트랙은 별도 코드베이스로 개발한다. 네이티브 앱은 브라우저·백엔드 서비스 없이 단독 설치·실행되며, TCP 어댑터·프레이머·로그 저장 로직을 자체적으로 구현한다.
- 기능 요구사항(6장), 상태 정의(5장), 예외·보안 요구사항(12장)은 두 트랙에 공통 적용되는 기준으로 삼는다. 화면 구성과 구현 기술은 트랙별로 다를 수 있다.
- 데이터 모델(9장)과 파일 형식(10장)은 두 트랙이 동일한 로그 조회·이관 경험을 제공하도록 가능한 한 동일 스키마/형식을 따른다. 트랙 간 DB 공유 여부(같은 PC에서 두 트랙을 함께 쓰는 경우 포함)는 17장 확정 후 별도 문서화한다.
- 네이티브 앱의 기술 스택, 배포 방식(설치 파일/포터블), 자동 업데이트 정책은 이 문서 범위 밖이며 별도 아키텍처 문서에서 다룬다.

## 2. MVP 범위

### 2.1 포함

- 로그인, 로그아웃, 세션 만료, 본인 비밀번호 변경
- Admin, Operator, Viewer 역할 기반 권한
- 사용자 등록·조회·수정·비활성화/삭제
- TCP Server/Client 설정, 시작·중지·재연결, 텍스트 송신
- IP/호스트명, 포트, 인코딩, 메시지 구분자, 제한시간 설정
- 실시간 터미널, 자동 스크롤, 검색·필터·일시정지·화면 지우기
- DB 로그 저장과 일자별 중복 없는 번호 부여
- TXT/XLSX 독립 저장 스위치, 저장 경로, 날짜별 파일, 실패 재시도
- 로그 목록·상세·검색·필터·권한 기반 다운로드
- 일정 등록·조회·수정·삭제와 완료/미완료 전환
- 대시보드, 시스템 상태, 중요 변경 감사 로그

### 2.2 MVP 제외 또는 후속

- 검사 이미지·영상 저장 및 뷰어, SPC/AI 재분석
- SMS·메신저·이메일 알림
- 다중 공장·다중 테넌트, 외부 SSO/AD 연동
- 자동 로그 삭제와 장기 아카이브(기본 비활성화)
- 사용자 정의 프로토콜 스크립트와 원격 장비 제어
- Serial(RS-232/485) 통신 지원 — MVP는 TCP만 지원(6.2절). J1C 참고 기능([www.j1lab.com](http://www.j1lab.com) J1C Ver 6.2.6 설명 참조)
- UDP 통신 지원, 송신 전용/수신 전용 포트 분리 옵션(UDP Option: receive only port)
- 자동응답(Auto Reply) — 수신 패턴 사전 정의 후 자동 송신, ASCII/HEX 구분, 매 세션 사용 여부 선택
- 타이머 반복/원싸이클 송신, 파일 라인 범위 지정 송신
- 에러 체크 모드(체크섬, CRC16, 범위 지정)
- 파일(File Data) 기반 프로토콜 송신 UI — 라인 더블클릭 송신, `;` 주석 규칙
- 전체 메뉴 단축키 목록화

## 3. 사용자와 권한

| 기능 | Admin | Operator | Viewer |
|---|:---:|:---:|:---:|
| 대시보드/터미널/로그/일정 조회 | O | O | O |
| TCP 시작·중지·재연결·송신 | O | O | X |
| 통신/저장 설정 변경 | O | O | X |
| 로그 다운로드 | O | O | 정책에 따라 허용 |
| 일정 등록·수정·삭제·완료 | O | O | X |
| 사용자 CRUD/감사 로그 | O | X | X |

계정 규칙:

- 로그인 ID는 정규화 후 중복을 허용하지 않는다.
- 비밀번호는 평문으로 저장하지 않고 Argon2id 또는 bcrypt로 해시한다.
- 연속 실패 횟수를 제한하고 잠금 상태와 해제 시각을 기록한다.
- 마지막 활성 Admin과 현재 로그인 사용자는 삭제/비활성화할 수 없다.
- 물리 삭제보다 비활성화/소프트 삭제를 기본으로 감사 추적성을 보존한다.
- 모든 API는 메뉴 노출 여부와 무관하게 서버에서 권한을 다시 검사한다.

## 4. 시스템 구조

### 4.1 웹 브라우저 Raw TCP 제약

일반 웹 브라우저는 보안 샌드박스 때문에 임의의 Raw TCP 소켓을 직접 열어 비전 장비에 연결하거나 포트를 열고 수신 대기할 수 없다. 브라우저의 WebSocket은 Raw TCP와 다른 프로토콜이므로, 장비가 일반 TCP만 제공한다면 브라우저가 장비와 직접 통신할 수 없다.

따라서 TCP 통신은 현장 PC에서 실행되는 **백엔드 서비스**가 담당한다. 브라우저는 백엔드와 HTTPS 및 WebSocket/SSE로 통신한다.

~~~text
┌─────────────────┐       Raw TCP/IP      ┌─────────────────────────────┐
│ 비전 시스템      │ ◀────────────────────▶ │ 백엔드 Log Service           │
└─────────────────┘                       │ ┌─────────────────────────┐ │
                                          │ │ TCP Adapter             │ │
                                          │ │ Server / Client         │ │
                                          │ └──────────┬──────────────┘ │
                                          │            ▼                │
                                          │ Parse → DB → Export Queue   │
                                          │              ├─ TXT         │
                                          │              └─ XLSX        │
┌─────────────────┐ HTTPS + WS/SSE       │ REST API / Realtime Hub     │
│ 웹 브라우저 UI   │ ◀────────────────────▶ │ Auth / Schedule / User      │
└─────────────────┘                       └─────────────────────────────┘
~~~

### 4.2 백엔드 책임과 처리 흐름

- TCP 수신 대기 또는 대상 서버 연결, 연결 상태/재접속 관리
- TCP 스트림을 메시지로 재조립하고 인코딩·길이·구분자를 검증
- 송수신 원문과 메타데이터를 DB에 먼저 기록
- 일자별 번호를 DB 트랜잭션으로 원자적으로 발급
- TXT/XLSX 저장을 별도 큐에서 수행하고 실패 시 재시도
- 신규 로그와 연결/저장 상태를 웹에 실시간 전달
- 인증, 권한, 일정, 사용자, 설정, 로그 조회 API 제공
- 재시작 시 설정과 미완료 저장 작업 복원

권장 순서는 TCP 수신/송신 → 메시지 검증 → DB/번호 저장 → 웹 이벤트 → 파일 큐 → TXT/XLSX 기록 → 형식별 상태 갱신이다. DB를 기준 원본으로, 파일을 재생성 가능한 산출물로 취급한다.

## 5. 상태 정의

### 5.1 연결 상태 connection_status

| 값 | 화면 표시 | 의미 |
|---|---|---|
| STOPPED | 중지 | 아직 시작하지 않았거나 사용자가 중지 |
| LISTENING | 수신 대기 | Server 모드에서 접속 대기 |
| CONNECTING | 연결 중 | Client 모드 접속 시도 |
| CONNECTED | 연결됨 | 통신 가능한 소켓 존재 |
| RECONNECTING | 재연결 중 | 자동 재접속 시도 |
| ERROR | 오류 | 시작/연결 실패, 조치 필요 |

### 5.2 로그와 처리 상태

- direction: RX(장비→프로그램), TX(프로그램→장비)
- communication_status: QUEUED, PROCESSING, COMPLETED, FAILED
- file_save_status: DISABLED, PENDING, WRITING, SAVED, FAILED
- schedule_status: OPEN, COMPLETED

RX의 COMPLETED는 완전한 메시지가 DB에 수용되었음을, TX의 COMPLETED는 OS 소켓 쓰기가 성공했음을 뜻한다. 이는 상대 장비의 업무 처리 완료를 보장하지 않는다. 장비 ACK까지 완료로 볼지는 프로토콜 확정 후 추가한다.

**통신 상태와 파일 저장 상태는 결합하지 않는다.** 한 로그가 통신 COMPLETED, TXT SAVED, XLSX FAILED일 수 있다. 화면과 DB 모두 통신, TXT, XLSX 상태를 별도 열/필드로 관리한다.

## 6. 기능 요구사항

### 6.1 인증과 사용자 CRUD

- AUTH-01: ID/PASSWORD 로그인, 로그아웃, 유휴 세션 만료를 지원한다.
- AUTH-02: 비활성·삭제·잠금 계정의 로그인을 거부한다.
- AUTH-03: 본인 비밀번호 변경 시 현재 비밀번호를 재확인한다.
- AUTH-04: 쿠키 세션은 HttpOnly, Secure, SameSite 및 CSRF 방어를 적용한다.
- USR-01: Admin은 사용자 ID, 이름, 역할, 활성 상태를 지정해 등록한다.
- USR-02: Admin은 사용자 검색·조회·수정·비활성화/삭제와 비밀번호 초기화를 수행한다.
- USR-03: 중복 ID, 마지막 Admin 삭제, 본인 삭제를 서버에서 거부한다.
- USR-04: 생성·수정·비활성화·비밀번호 초기화 이력을 감사 로그에 남긴다.

### 6.2 TCP 공통

- TCP-01: Server/Client 모드를 선택하고 연결을 시작·중지·재연결한다.
- TCP-02: 포트는 1~65535 정수만 허용한다.
- TCP-03: 인코딩, CR/LF/CRLF/사용자 구분자, 최대 메시지 크기, 연결/읽기 제한시간을 설정한다. 고정 길이는 장비 요구 확인 후 포함한다.
- TCP-04: 패킷이 나뉘거나 여러 메시지가 한 패킷에 합쳐져도 프레이머가 정확히 분리한다.
- TCP-05: 핵심 설정을 실행 중 변경할 때 영향 안내와 확인 후 연결을 재시작한다.
- TCP-06: 현재 상태, 상대 주소, 마지막 오류, 마지막 송수신 시각을 표시한다.
- TCP-07: 송신 입력창에서 텍스트를 보내고 TX 로그와 성공/실패를 기록한다.

### 6.3 Server 모드

- 바인딩 IP와 수신 포트를 설정하고 시작 시 LISTENING을 표시한다.
- 포트 사용 중, 권한 부족, 잘못된 바인딩 IP를 구분해 알린다.
- 단일/복수 클라이언트 정책을 설정 가능하게 설계하며 MVP 기본은 단일이다.
- 상대 IP/포트와 접속·해제 시각을 기록한다.
- 복수 허용 시 연결 ID를 로그에 남기고 TX 대상 선택을 요구한다.

### 6.4 Client 모드

- 대상 IP/호스트명과 포트를 설정한다.
- 연결 실패/단절 시 설정에 따라 자동 재접속한다.
- 재시도 횟수와 다음 시도 시각을 표시하고, 사용자 중지 시 재접속하지 않는다.
- DNS 실패, 연결 거부, 제한시간 초과를 구분해 기록한다.

### 6.5 실시간 터미널

- 발생 시각, 번호, 방향, 원문, 통신 상태, TXT 상태, XLSX 상태를 표시한다.
- 자동 스크롤, 표시 일시정지, 화면 지우기, 문자열 검색, 방향/상태 필터를 제공한다.
- 일시정지는 브라우저 렌더링만 멈추며 수신/저장은 계속한다.
- 화면 지우기는 현재 브라우저 표시만 비우고 DB/파일을 삭제하지 않는다.
- 표시 최대 건수를 제한해 장시간 실행 시 성능 저하를 방지한다.

### 6.6 로그와 파일 저장

- LOG-01: 유효한 TX/RX를 DB에 저장하고 기본 YYYYMMDD-000001 형식의 일자별 번호를 중복 없이 발급한다.
- LOG-02: TXT/XLSX 저장 스위치를 독립 제공한다. 둘 다 꺼져도 DB와 화면 모니터링은 계속한다.
- LOG-03: 저장 경로는 백엔드 PC가 접근 가능한 허용 루트로 제한한다. 원격 브라우저의 로컬 폴더를 서버 경로로 직접 쓸 수 없음을 안내한다.
- LOG-04: 경로 존재, 쓰기 권한, 디스크 공간을 검사한다.
- LOG-05: Asia/Seoul 기준 날짜별 파일을 생성하고 자정에 전환한다.
- LOG-06: 기본 파일명은 VisionLog_YYYY-MM-DD.txt, VisionLog_YYYY-MM-DD.xlsx이다.
- LOG-07: 번호, 원문 데이터, 일정 ID/제목, 발생 일시, 방향, 통신 상태를 기록한다.
- LOG-08: TXT/XLSX별 저장 상태, 저장 시각, 시도 횟수, 오류를 별도로 남긴다.
- LOG-09: 실패는 DB 큐에서 재시도하며 TCP 수신을 막지 않는다. 자동/수동 재시도를 지원한다.
- LOG-10: XLSX 쓰기는 직렬화하고 파일 잠금 시 해당 형식만 실패·재시도한다.
- LOG-11: XLSX 행/파일 크기 기준을 넘으면 _part02 등으로 분할한다.
- LOG-12: 종료 시 제한시간 동안 큐를 비우고 남은 작업은 재시작 후 처리한다.
- LOG-13: 자동 삭제는 기본 비활성화하며 향후 활성화 시 Admin 권한과 감사 기록을 적용한다.

### 6.7 로그 조회

- 기간, 번호, 방향, 통신 상태, TXT/XLSX 상태, 데이터 문자열, 일정으로 검색한다.
- 페이지 조회와 정렬을 지원하고 상세에서 원문, 연결, 오류, 형식별 저장 이력을 보여 준다.
- 권한 있는 사용자는 조회 결과 또는 날짜별 파일을 다운로드할 수 있으며 이를 감사 기록한다.

### 6.8 일정 CRUD와 완료

- Admin/Operator는 제목, 내용, 예정 일시, 담당자로 일정을 등록·조회·수정·삭제한다.
- 기간, 완료 상태, 작성자/담당자, 검색어로 목록을 필터한다.
- 완료 처리 시 상태, 완료 일시, 완료 처리자를 기록하며 완료 취소도 감사 기록한다.
- 연결 로그가 있으면 소프트 삭제하거나 삭제를 막아 참조 무결성을 보존한다.
- MVP는 사용자가 활성 일정을 선택해 이후 로그에 연결하는 수동 방식을 기본으로 한다.
- 과거 예정일은 경고하되 허용하고, 완료 시각은 서버 시각으로 기록한다.

### 6.9 대시보드와 운영 설정

- 모드, 연결 상태, 가동 시간, 상대 주소, 마지막 송수신을 표시한다.
- 당일 RX/TX/통신 실패와 TXT/XLSX별 성공·대기·실패를 표시한다.
- 최근 로그, 오늘/미완료 일정, 저장 경로, 디스크 여유, 최근 파일 저장을 표시한다.
- 설정 변경은 권한·값 검증·감사 기록을 거치고 저장 상태와 실제 적용 상태를 구분한다.

## 7. 화면 목록

| 화면 | 주요 내용 | 접근 역할 |
|---|---|---|
| 로그인 | ID/PASSWORD, 오류, 잠금 안내 | 전체 |
| 대시보드 | 연결, 당일 집계, 저장 상태, 최근 로그, 일정 | 전체 |
| 통신 모니터 | 터미널, TX 입력, 시작/중지, 검색/필터 | 전체, 제어는 Admin/Operator |
| 통신 설정 | Server/Client, 주소, 포트, 프레이밍, 재접속 | Admin/Operator |
| 저장 설정 | TXT/XLSX 스위치, 경로, 파일명, 재시도 | Admin/Operator |
| 로그 조회 | 검색, 상세, 다운로드 | 전체, 다운로드는 권한별 |
| 일정 관리 | 등록, 목록, 수정, 삭제, 완료 | 전체, 편집은 Admin/Operator |
| 사용자 관리 | 사용자 CRUD, 역할, 활성화, 암호 초기화 | Admin |
| 감사 로그 | 로그인·사용자·설정·다운로드 이력 | Admin |
| 시스템 상태 | 서비스, 큐, DB, 디스크, 최근 오류 | Admin/Operator |

## 8. 대시보드 ASCII 와이어프레임

~~~text
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│ VISION LOG PROGRAM                                  2026-08-22 14:32:10 [Operator ▾] [로그아웃]│
├───────────────┬──────────────────────────────────────────────────────────────────────────────┤
│ ● 대시보드    │ 통신 상태                                                                     │
│ ○ 모니터      │ ┌──────────────────┬─────────────────────┬────────────────────────────────┐ │
│ ○ 로그 조회   │ │ MODE: SERVER     │ ● 연결됨            │ 0.0.0.0:5000 / 192.168.0.20  │ │
│ ○ 일정 관리   │ │ 가동 03:21:08    │ 최근 RX 14:32:09    │ [중지] [재연결]               │ │
│ ○ 통신 설정   │ └──────────────────┴─────────────────────┴────────────────────────────────┘ │
│ ○ 저장 설정   │                                                                               │
│ ○ 사용자 관리 │ 오늘의 처리량                         파일 저장                               │
│ ○ 감사 로그   │ ┌──────────┬──────────┬──────────┐    ┌────────────────┬────────────────┐ │
│               │ │ RX 12,480│ TX 120   │ 실패 3   │    │ TXT ● ON      │ XLSX ● ON     │ │
│               │ └──────────┴──────────┴──────────┘    │ 저장 12,597    │ 저장 12,594   │ │
│               │                                       │ 실패 0         │ 실패 3 [재시도]│ │
│               │                                       └────────────────┴────────────────┘ │
│               │                                                                               │
│               │ 최근 로그                                                                     │
│               │ ┌───────────────┬────────┬────┬──────────────────┬────────┬──────┬──────┐ │
│               │ │ 번호          │ 시각   │방향│ 데이터           │ 통신   │ TXT  │ XLSX │ │
│               │ ├───────────────┼────────┼────┼──────────────────┼────────┼──────┼──────┤ │
│               │ │20260822-012600│14:32:09│ RX │OK,PART-001,...   │✓ 완료  │✓ 저장│! 실패│ │
│               │ │20260822-012599│14:32:08│ TX │ACK               │✓ 완료  │✓ 저장│✓ 저장│ │
│               │ └───────────────┴────────┴────┴──────────────────┴────────┴──────┴──────┘ │
│               │                                                                               │
│               │ 일정                                     시스템                               │
│               │ [ ] 15:00 라인 A 점검                    DB 정상 · 디스크 68% 여유              │
│               │ [✓] 10:00 모델 교체 완료                 저장 대기 3건 · 최근 오류 14:32:09     │
└───────────────┴──────────────────────────────────────────────────────────────────────────────┘
~~~

상태는 색상만 사용하지 않고 ● 연결됨, ✓ 저장, ! 실패처럼 아이콘과 텍스트를 함께 표시한다.

## 9. 데이터 모델

### 9.1 사용자 users

| 필드 | 형식 | 필수 | 설명/제약 |
|---|---|:---:|---|
| id | UUID/정수 | O | 내부 고유 ID |
| login_id | 문자열 | O | 정규화 후 고유 |
| password_hash | 문자열 | O | 평문 금지 |
| display_name | 문자열 | O | 표시 이름 |
| role | 열거형 | O | ADMIN, OPERATOR, VIEWER |
| is_active | 불리언 | O | 로그인 허용 |
| failed_login_count | 정수 | O | 연속 실패 횟수 |
| locked_until | 일시 | X | 잠금 해제 시각 |
| last_login_at | 일시 | X | 마지막 성공 로그인 |
| created_at, updated_at | 일시 | O | 서버 기준 |
| deleted_at | 일시 | X | 소프트 삭제 |

### 9.2 로그 log_records

| 필드 | 형식 | 필수 | 설명/제약 |
|---|---|:---:|---|
| id | UUID/정수 | O | 내부 고유 ID |
| daily_date | 날짜 | O | Asia/Seoul 기준 |
| daily_sequence | 정수 | O | 날짜 내 증가, 날짜와 복합 고유 |
| display_number | 문자열 | O | 예: 20260822-000001, 고유 |
| occurred_at | 일시 | O | 송수신 발생 시각 |
| direction | 열거형 | O | TX 또는 RX |
| raw_data | 텍스트/바이너리 | O | 원본 보존 |
| display_text | 텍스트 | O | 안전하게 디코딩한 표시 문자열 |
| communication_status | 열거형 | O | 통신 상태 |
| communication_error | 문자열 | X | 통신 실패 코드/요약 |
| connection_profile_id | FK | O | 사용 연결 설정 |
| connection_session_id | FK/문자열 | X | 접속 세션 |
| peer_address, peer_port | 문자열/정수 | X | 상대 주소 |
| schedule_id | FK | X | 연결 일정 |
| created_at | 일시 | O | DB 생성 시각 |

권장 인덱스는 occurred_at, daily_date+daily_sequence 고유, direction+occurred_at, communication_status+occurred_at, schedule_id이다.

### 9.3 파일 저장 결과 file_write_results

| 필드 | 형식 | 필수 | 설명/제약 |
|---|---|:---:|---|
| id | UUID/정수 | O | 내부 ID |
| log_record_id | FK | O | 대상 로그 |
| format | 열거형 | O | TXT 또는 XLSX |
| save_status | 열거형 | O | DISABLED/PENDING/WRITING/SAVED/FAILED |
| target_path | 문자열 | X | 실제 파일 |
| attempt_count | 정수 | O | 저장 시도 수 |
| next_retry_at | 일시 | X | 다음 재시도 |
| saved_at | 일시 | X | 성공 시각 |
| error_code, error_message | 문자열 | X | 실패 정보 |
| created_at, updated_at | 일시 | O | 이력 시각 |

log_record_id+format은 고유하다. 통신 상태는 이 테이블에 저장하지 않는다.

### 9.4 일정 schedules

| 필드 | 형식 | 필수 | 설명/제약 |
|---|---|:---:|---|
| id | UUID/정수 | O | 내부 ID |
| title | 문자열 | O | 제목 |
| description | 텍스트 | X | 내용 |
| scheduled_at | 일시 | O | 예정 일시 |
| status | 열거형 | O | OPEN 또는 COMPLETED |
| assignee_user_id | FK | X | 담당자 |
| created_by | FK | O | 작성자 |
| completed_at | 일시 | X | 완료 시각 |
| completed_by | FK | X | 완료 처리자 |
| created_at, updated_at | 일시 | O | 생성/수정 시각 |
| version | 정수 | O | 동시 수정 충돌 감지 |
| deleted_at | 일시 | X | 소프트 삭제 |

### 9.5 추가 엔터티와 관계

- connection_profiles: 모드, 주소, 포트, 인코딩, 구분자, 제한시간, 재접속, 적용 상태
- connection_sessions: 연결 시작/종료, 상대 주소, 종료 사유, 마지막 송수신
- storage_settings: 허용 저장 경로, TXT/XLSX 스위치, 파일명, 시간대, 보존 정책
- daily_sequences: 날짜별 마지막 번호를 원자적으로 관리
- audit_logs: 사용자, 작업, 대상, 변경 요약, IP, 성공 여부, 일시

~~~text
users 1 ── N schedules
users 1 ── N audit_logs
schedules 1 ── N log_records       (선택 연결)
connection_profiles 1 ── N connection_sessions
connection_profiles 1 ── N log_records
connection_sessions 1 ── N log_records
log_records 1 ── 0..2 file_write_results (TXT/XLSX 각각 최대 1건)
~~~

## 10. 파일 형식 규칙

### 10.1 TXT

- 기본 UTF-8, 한 로그 한 행으로 기록한다.
- 데이터 내 CR/LF는 이스케이프하거나 길이 기반 형식으로 보존한다.
- 필드는 탭 또는 설정된 구분자로 기록하고 같은 파일 쓰기는 직렬화한다.

### 10.2 XLSX

- 헤더: 번호, 데이터, 일정 ID, 일정, 발생 일시, 방향, 통신 상태.
- 데이터 셀을 문자열 형식으로 저장한다.
- =, +, -, @로 시작하는 값은 수식으로 실행되지 않게 문자열 셀 타입을 강제한다. CSV가 추가되면 선행 아포스트로피 등 별도 방어를 적용한다.
- DB 원본은 변형하지 않고 안전 처리는 파일 표현 계층에만 적용한다.
- 파일 잠금, 손상, 행 수 한계를 감지하고 재시도 또는 분할한다.

### 10.3 날짜 전환

- 파일 날짜와 번호는 메시지 처리 시점의 Asia/Seoul 날짜를 사용한다.
- 자정 동시 메시지는 DB 트랜잭션과 단일 파일 워커로 정확한 날짜에 배치한다.
- 시스템 시각이 뒤로 바뀌어도 고유 제약으로 번호 중복을 막는다.

## 11. 디자인 시스템

### 11.1 원칙

- 회색 배경에 Cognex 로고 색을 연상시키는 노란색을 주요 글자, 테두리, 선택 상태에 쓴다.
- 순수 검정/순수 노랑의 과도한 대비를 피하고 긴 로그의 가독성을 확보한다.
- 산업용 PC 1366×768 이상을 우선하고 1920×1080에서 정보 밀도를 최적화한다.
- 테이블 헤더 고정, 행 가상화, 명확한 키보드 포커스를 지원한다.
- 성공/경고/실패는 색뿐 아니라 아이콘과 텍스트를 병기한다.

### 11.2 디자인 토큰 초안

~~~css
--color-bg:             #202225;
--color-surface:        #2B2E33;
--color-surface-raised: #35393F;
--color-border:         #FFD400;
--color-accent:         #FFD400;
--color-accent-hover:   #FFE45C;
--color-text-primary:   #FFF3A6;
--color-text-muted:     #C7BE78;
--color-success:        #67D391;
--color-warning:        #FFB74D;
--color-danger:         #FF6B6B;
--color-info:           #64B5F6;
--radius-sm:            4px;
--radius-md:            8px;
--space-unit:           4px;
~~~

## 12. 예외·보안·엣지 케이스

| 상황 | 요구 동작 |
|---|---|
| 잘못된 IP/호스트/포트 | 저장 전 검증, 필드 단위 오류 |
| 사용 중인 Server 포트 | 시작 실패, ERROR, 안전한 OS 오류 요약 |
| 실행 중 설정 변경 | 영향 안내 후 확인을 받아 재시작 |
| 네트워크 단절/상대 종료 | 즉시 상태 갱신, 원인 기록, 설정 시 자동 재접속 |
| 반복 접속/단절 | 재시도 상한과 로그 억제로 폭주 방지 |
| TCP 패킷 분할/병합 | 누적 버퍼와 프레이머로 완전한 메시지 생성 |
| 빈 데이터/잘못된 인코딩 | 정책에 따라 무시 또는 실패 로그 |
| 과대 메시지 | 최대 크기에서 차단, 연결 종료 여부 정책화 |
| 제어문자/바이너리 | 원본 보존과 안전한 화면 표현 분리 |
| 빠른 연속 데이터 | 유한 큐, 배압, DB 배치, UI 표시 제한 |
| 복수 클라이언트 | 연결 ID와 원자적 번호로 출처·순서 보존 |
| 중복 메시지 | 기본 별도 기록, 장비 메시지 ID 존재 시 정책 추가 |
| 경로 없음/권한 없음 | DB 지속, 해당 형식 FAILED, 경고·재시도 |
| 디스크 부족/공유 드라이브 단절 | 큐 유지, TCP 지속, 운영 경고 |
| XLSX 잠금 | XLSX만 실패, 해제 후 재시도 |
| 저장 중 강제 종료 | PENDING/WRITING을 재시작 시 복구 |
| 날짜 변경 중 수신 | 처리 시각과 트랜잭션으로 파일/번호 결정 |
| Excel 수식 삽입 | 문자열 셀 강제, DB 원본 유지 |
| HTML/script 수신(XSS) | HTML 삽입 금지, 텍스트 이스케이프, CSP |
| 경로 조작 | 허용 루트 밖 .., 링크 우회, 시스템 경로 차단 |
| 반복 로그인 실패 | 계정/IP별 제한, 일시 잠금, 감사 로그 |
| 중복 ID/마지막 Admin/본인 삭제 | 서버 고유 제약과 보호 규칙으로 거부 |
| 동시 일정/설정 수정 | 버전 기반 낙관적 잠금과 충돌 안내 |
| 과거 일정/완료 모순 | 과거 일정 경고, 완료 필드 서버 검증 |

추가 보안 요구사항:

- 브라우저 입력과 TCP 데이터는 신뢰하지 않고 길이·형식·권한을 검증한다.
- 화면은 textContent 또는 동등한 안전 렌더링을 사용하고 임의 HTML 실행을 금지한다.
- 운영은 HTTPS/WSS와 CSP, frame-ancestors 등 보안 헤더를 사용한다.
- 로그 다운로드에 인증·권한·감사 기록을 적용한다.
- 비밀번호, 세션 토큰, 원문 데이터가 진단 로그에 불필요하게 남지 않게 한다.
- 저장 경로와 DB에 OS 최소 권한을 적용하고 오류에 스택·내부 경로·비밀값을 노출하지 않는다.

## 13. 비기능 요구사항

### 13.1 신뢰성과 무결성

- 브라우저 접속 여부와 무관하게 TCP 수신과 저장이 계속되어야 한다.
- DB 저장을 파일보다 먼저 끝내고 파일 작업은 재시도 가능한 큐로 관리한다.
- 재시작 후 사용자, 일정, 설정, 로그, 번호, 미완료 파일 작업을 복원한다.
- 일자별 번호와 형식별 저장 결과에 DB 고유 제약을 둔다.
- 정상 종료 시 신규 수신을 멈춘 뒤 대기 작업을 안전하게 마무리한다.

### 13.2 성능 잠정 기준

- 장비 사양 확정 전 MVP 기준은 평균 메시지 2 KB, 지속 100건/초, 순간 500건/초로 가정한다.
- 정상 부하에서 백엔드 수용 후 1초 이내(p95) 터미널 표시를 목표로 한다.
- 10만 건 이상에서 일반 검색 API 2초 이내(p95)를 목표로 페이지 조회한다.
- 브라우저 터미널에는 기본 최근 1,000건만 유지한다.
- 파일 지연 시 메모리에 무한 적재하지 않고 DB 기반 큐를 사용한다.

이 수치는 실제 예상 처리량과 장비 성능 확인 후 조정한다.

### 13.3 가용성·복구·관측

- OS 시작 시 백엔드 자동 실행과 비정상 종료 후 자동 재시작을 지원한다.
- DB/설정 백업 및 복원 절차를 문서화한다.
- 서비스, DB, TCP, 파일 큐, 디스크 상태 확인 기능을 제공한다.
- 통신량, 실패량, 큐 길이, 저장 지연, 재접속 수를 집계한다.
- 사용자/권한/설정 변경과 다운로드를 감사 기록한다.
- 반복 오류는 집계/억제해 UI와 진단 로그 폭주를 막는다.

### 13.4 사용성·유지보수

- 사용자가 3초 내에 연결 상태와 저장 장애를 식별할 수 있어야 한다.
- 오류는 원인, 영향, 가능한 조치를 함께 제공한다.
- 색상에만 의존하지 않고 키보드 포커스와 충분한 대비를 제공한다.
- 최신 Chromium 기반 Edge/Chrome을 우선 지원한다.
- TCP Adapter, DB, 파일 Export, Web API를 분리하고 통신/파일 규칙을 자동 테스트한다.
- DB 마이그레이션과 설정 버전 관리를 지원하며 비밀값을 코드에 하드코딩하지 않는다.

## 14. 기본값

| 항목 | MVP 기본값 |
|---|---|
| 시간대 | Asia/Seoul |
| 통신 모드 | SERVER |
| Server 바인딩 IP | 0.0.0.0 |
| Client 대상 | 미입력, 사용자 필수 입력 |
| 포트 | 5000 |
| 인코딩 | UTF-8 |
| 구분자 | CRLF |
| 최대 메시지 | 1 MiB |
| 연결 제한시간 | 10초 |
| 읽기 유휴 제한시간 | 30초, 종료 여부 설정 가능 |
| Server 동시 클라이언트 | 1개 |
| Client 자동 재접속 | 켜짐 |
| 재접속 | 5초 시작, 최대 60초 |
| TXT 저장 | 켜짐 |
| XLSX 저장 | 꺼짐 |
| 저장 경로 | 백엔드 데이터 디렉터리 아래 logs |
| 파일명 | VisionLog_YYYY-MM-DD.{txt,xlsx} |
| 번호 | 일자별 초기화, YYYYMMDD-000001 |
| 자동 스크롤 | 켜짐 |
| 터미널 최대 표시 | 1,000건 |
| 일정-로그 연결 | 활성 일정 수동 선택 |
| 로그 자동 삭제 | 꺼짐 |
| 세션 유휴 만료 | 30분 |
| 로그인 잠금 | 5회 실패 시 15분 |
| 파일 재시도 | 1, 5, 15, 30, 60초 후 반복, 수동 가능 |

초기 Admin 비밀번호는 설치 시 생성하거나 최초 실행에서 설정한다. 코드나 문서에 고정 비밀번호를 넣지 않는다.

## 15. API/실시간 경계 제안

- 인증: /api/auth/login, /api/auth/logout, /api/auth/me, /api/auth/password
- 통신: /api/connection/status, /start, /stop, /send
- 설정: /api/settings/connection, /api/settings/storage
- 로그: /api/logs, /api/logs/{id}, /api/logs/export, /api/file-jobs/retry
- 일정: /api/schedules, /api/schedules/{id}, /complete
- 사용자: /api/users, /api/users/{id}
- 운영: /api/health, /api/audit-logs
- 실시간 이벤트: connection.status, log.created, log.communication.updated, log.file.updated, system.alert

URL은 구현에 따라 바꿀 수 있으나 통신 상태 이벤트와 파일 상태 이벤트는 분리한다.

## 16. 수용 기준

- [ ] Admin이 사용자를 등록하면 해당 계정으로 로그인할 수 있고 수정·비활성화가 즉시 반영된다.
- [ ] Operator/Viewer는 사용자 관리 API와 화면에 접근할 수 없다.
- [ ] 마지막 활성 Admin과 현재 로그인 사용자 삭제가 서버에서 거부된다.
- [ ] Server 모드를 시작하면 지정 포트에서 대기하고 LISTENING/CONNECTED가 표시된다.
- [ ] Client 모드는 대상에 연결하고 단절 시 설정대로 재접속한다.
- [ ] 브라우저를 닫아도 백엔드 TCP 수신과 DB/파일 저장이 계속된다.
- [ ] 분할 메시지와 한 패킷의 복수 메시지가 구분자 기준으로 정확히 기록된다.
- [ ] 메시지는 잠정 성능 기준 내에 시각, 번호, RX, 데이터, 상태와 함께 표시된다.
- [ ] 송신 시 TX 로그와 소켓 쓰기 성공/실패가 기록된다.
- [ ] TXT ON/XLSX OFF면 TXT만, 반대면 XLSX만 생성된다.
- [ ] 둘 다 ON이면 둘 다, 둘 다 OFF여도 DB와 화면 기록은 유지된다.
- [ ] 날짜 변경 시 이전 파일을 보존하고 새 날짜 파일/번호를 사용한다.
- [ ] 파일 행에 번호, 데이터, 일정, 일시, 방향, 통신 상태가 포함된다.
- [ ] 통신, TXT, XLSX 상태가 서로 독립적으로 조회된다.
- [ ] 경로/쓰기 실패에도 TCP 수신이 계속되고 해당 형식만 FAILED가 된다.
- [ ] 장애 해소 후 자동/수동 재시도로 SAVED가 되고 시도 이력이 남는다.
- [ ] XLSX가 잠겨도 TXT는 영향 없이 저장된다.
- [ ] 일정 등록·조회·수정·삭제·완료가 되고 완료 시각/처리자가 기록된다.
- [ ] 기간, 방향, 통신, TXT/XLSX, 일정, 문자열로 로그를 검색한다.
- [ ] HTML/script 수신 문자열은 실행되지 않고 원문 텍스트로 표시된다.
- [ ] =, +, -, @ 시작 데이터는 XLSX에서 수식으로 실행되지 않는다.
- [ ] 재시작 후 설정, 사용자, 일정, 로그, 번호, 대기 작업이 복원된다.
- [ ] 사용자/권한/설정 변경과 다운로드가 감사 로그에 남는다.
- [ ] 상태를 색상뿐 아니라 아이콘과 한글 텍스트로 식별한다.
- [ ] 회색 배경과 노란색 글자/테두리 토큰이 주요 화면에 일관 적용된다.

## 17. 개발 전 확정 사항

- [ ] J1C에서 반드시 유지할 화면, 단축키, 조작 순서
- [ ] 실제 비전 시스템 송수신 메시지 샘플
- [ ] 종료 규칙(CR/LF/CRLF/고정 길이/길이 헤더)
- [ ] 인코딩, 최대 길이, 바이너리 포함 여부
- [ ] 장비 ACK와 통신 COMPLETED 최종 판정
- [ ] Server 복수 클라이언트와 TX 대상 선택 방식
- [ ] Client 대상이 고정 IP인지 호스트명도 쓰는지
- [ ] 평균/최대 초당 메시지와 메시지 크기
- [ ] 일자별 번호 초기화와 사용자 지정 시작 번호 필요 여부
- [ ] 일정-로그 연결 기준(수동/시간대/작업 번호)
- [ ] XLSX만 필요한지 CSV도 허용할지, 열 순서/서식
- [ ] TXT 행 형식과 데이터 내부 줄바꿈 처리
- [ ] 운영 OS, 서비스 계정, 네트워크 공유 경로
- [ ] 보존 기간, 백업 위치, 자동 삭제 승인
- [ ] DB 종류, 동시 사용자, 다중 백엔드 여부
- [ ] Viewer 다운로드 허용 여부
- [ ] 저장 경로 UI가 허용 루트 탐색인지 직접 입력인지

## 18. 권장 개발 순서

1. **프로토콜/PoC**: 실제 장비 메시지, 프레이밍, Server/Client, 처리량 검증
2. **기반**: DB 스키마, 설정, 로그인, 역할, 감사 로그
3. **TCP 서비스**: Adapter, 상태 머신, 재접속, 송신, 안전 종료
4. **수집 파이프라인**: 검증, 번호, DB, 실시간 이벤트/터미널
5. **파일 Export**: 독립 스위치, 날짜 전환, 직렬 쓰기, 큐/복구
6. **업무 화면**: 대시보드, 조회/다운로드, 일정 CRUD, 사용자 CRUD
7. **보안/예외**: XSS, Excel 수식 삽입, 경로, 로그인 제한, 충돌
8. **시험**: 부하, 단절, 파일 잠금, 디스크 부족, 자정, 강제 종료
9. **현장 배포**: 자동 시작, 백업/복원, 초기 Admin, 운영 교육

## 19. 완료 정의(Definition of Done)

- [ ] 16장의 모든 MVP 수용 기준을 시험하고 결과를 기록했다.
- [ ] 실제 또는 동등한 장비로 Server/Client 통합 시험을 통과했다.
- [ ] 장시간/부하 시험에서 데이터 유실, 번호 중복, UI 메모리 증가가 허용치 이내다.
- [ ] 단절, 포트 충돌, 파일 잠금, 경로 권한, 디스크 부족, 강제 종료 복구 시험을 통과했다.
- [ ] XSS, Excel 수식 삽입, 인증 우회, 경로 조작 시험을 통과했다.
- [ ] 역할별 화면/API 권한 시험을 통과했다.
- [ ] DB 백업/복원과 미완료 저장 작업 복구가 검증되었다.
- [ ] 설치, 초기 설정, 운영, 장애 대응, 로그 복구 매뉴얼이 제공되었다.
- [ ] 운영 PC 자동 시작, 시간대, 저장 권한, 방화벽 포트를 확인했다.
- [ ] 개발 전 확정 사항을 최종 사양/변경 이력에 반영했다.
- [ ] 제품 책임자와 현장 사용자가 주요 화면과 업무 흐름을 승인했다.

---

이 문서는 구현 기술 확정 전의 MVP 기준안이다. 실제 비전 시스템 메시지와 현장 조건을 확인한 뒤 통신 프로토콜, 처리량, 저장 형식, 일정 연결 규칙을 동결하고 개발 기준 버전으로 갱신한다.
